let x = 5;
let z = x ** 2; //The exponentiation operator (**) raises the first operand to the power of the second operand.
console.log(z);

{
    let x = 5;
    let z = Math.pow(x,2); // x ** y produces the same result as Math.pow(x,y):
    console.log(z)
}